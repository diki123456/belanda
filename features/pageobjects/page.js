module.exports = class Page {
    open(path) {
        return browser.url(`https://www.demoblaze.com/${path}`)
    }
}


module.export = class page {
    open(path) {
        return browser.url('https://demoqa.com/${path}')
    }
}